-- Prototype-specific tuning values. Edit here, not in logic scripts.
return {
	SnapRadius         = 3,     -- studs proximity for Sponge/Cloth snap to sofa cushion
	PrototypeParTime   = 60,    -- seconds par time for one Starter sofa
	GradeThresholds    = { Perfect = 0.80, Good = 1.00, Pass = 1.40 },
	ToolboxUnpackRange = 8,     -- studs max distance to trigger toolbox unpack
}
